# dependencies
/node_modules
/.npm-cache

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# local env files
.env*
!.env.example

# Sentry Config File
.env.sentry-build-plugin
.env.local

# Local Claude tooling — should never be committed
.agents/
.claude/skills/
.claude/worktrees/
.claude/launch.json

# test output
/playwright-report/
/test-results/
/coverage/

# IDE
*.tsbuildinfo


# Golden set — real customer review text. The labelled file stays local.
# Only the label-only projection (review_id, issue_id, language_bucket, …)
# may be committed. See docs/GOLDEN_SET.md.
/eval/*.csv

# clerk configuration (can include secrets)
/.clerk/
