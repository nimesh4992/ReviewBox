import { SentimentScreen } from "@/features/sentiment/components/sentiment-screen";

export const metadata = { title: "Sentiment" };

export default function SentimentPage() {
  return <SentimentScreen />;
}
